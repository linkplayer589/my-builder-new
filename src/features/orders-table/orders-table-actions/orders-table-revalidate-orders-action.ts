/**
 * Re-exports the revalidate orders action from the database layer
 * 
 * @description
 * This is a convenience re-export to maintain backward compatibility
 * and provide a feature-specific import path for revalidating orders.
 */
export { revalidateOrders } from "@/db/server-actions/order-actions/revalidate-orders"

