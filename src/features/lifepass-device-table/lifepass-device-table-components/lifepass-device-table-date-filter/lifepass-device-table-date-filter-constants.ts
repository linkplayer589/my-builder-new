/**
 * Constants used by the lifepass device table date filter.
 *
 * @description
 * These identifiers ensure both the client date filter control and the server
 * search-params parser reference the same filter entries when syncing the
 * selected date range through the URL `filters` parameter.
 */
export const LIFEPASS_DEVICE_TABLE_DATE_FROM_ROW_ID =
  "lifepass-device-table-date-from"
export const LIFEPASS_DEVICE_TABLE_DATE_TO_ROW_ID =
  "lifepass-device-table-date-to"
export const LIFEPASS_DEVICE_TABLE_DATE_FILTER_ID = "createdAt"
export const LIFEPASS_DEVICE_TABLE_DATE_PLACEHOLDER = "Choose a Date Range"


