/**
 * Shared constants for the sessions date filter.
 *
 * @description
 * These constants ensure that both the client-side date range picker and
 * the server component use the same identifiers when reading/writing date
 * filters inside the `filters` search parameter.
 */
export const SESSIONS_DATE_FROM_ROW_ID = "sessions-date-from"
export const SESSIONS_DATE_TO_ROW_ID = "sessions-date-to"
export const SESSIONS_DATE_FILTER_ID = "sessions_createdAt"
export const SESSIONS_DATE_PLACEHOLDER = "Choose a Date Range"


